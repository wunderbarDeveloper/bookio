import { SelectedBookPipe } from './selected-book.pipe';

describe('SelectedBookPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedBookPipe();
    expect(pipe).toBeTruthy();
  });
});
